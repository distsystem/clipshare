[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "clipshare"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "aiosqlite",
    "cryptography",
    "pydantic",
    "platformdirs",
    "zeroconf",
]
